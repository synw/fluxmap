/// Default alive timeout
const Duration defaultKeepAlive = Duration(seconds: 5);

/// Default disconnected timeout
const Duration defaultSleepingTimeout = Duration(seconds: 10);

/// Default max reasonable speed
const double maxReasonableSpeed = 400.0;
